#include <iostream>
#include <algorithm>
#include<stdio.h>
using namespace std;


int main()
{   //int a,b;
    // cin>> a>> b;
    // int c = min(a,b);
     //int d = max(a,b);
     //cout << c  << " "<<  d << endl;
    int a,b;
    cin >> a >> b;
    swap(a,b);
    cout << a <<" " << b << endl;
    return 0;
}